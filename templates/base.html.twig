<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <header>
            <nav>
                <p><a href="{{ path('categorie') }}">Catégories</a></p>
                <p><a href="{{ path('post') }}">Articles</a></p>
                <p><a href="{{ path('saveCategorie') }}">SaveCategory</a></p>
                <p><a href="{{ path('savePost') }}">SaveArticle</a></p>
            </nav>
        </header>
        {% block body %}{% endblock %}
    </body>
</html>
